<!-- First line, no heading: Closes #<ticket>. Delete only if this closes nothing. -->

## What

<!-- What changes and why. Bullets, one per change; a paragraph only when a
     single point needs it. -->

## Design

<!-- Why this shape: the constraint or trade-off that decided it, and the
     alternative you rejected. Bullets, one per decision. Docs record only
     current state, so this is where the reasoning lives. Delete for a
     mechanical change. -->

## Testing

<!-- What CI cannot prove. Bullets, one per check: cases exercised by hand and
     where, plus the stories or tests added or updated and anything left
     uncovered and why. CI proves the suite passed, not that the new behaviour
     is in it.

     `pnpm check`, `pnpm build`, `pnpm test`, `pnpm test:storybook` and
     `pnpm test:integration` run on every push — the checks list already reports
     them, so this section stays silent about them. -->
