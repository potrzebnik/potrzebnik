<!-- First line, no heading: Closes #<ticket>. Delete only if this closes nothing. -->

## What

<!-- The change in the reviewer's terms: what behaviour is different, why,
     and how. One bullet per change, not per file. Name a thing, don't locate
     it: a skill by its name, a config change by the option it adds, a docs
     change by its subject — the diff gives the paths. Name a path only when
     the path is the point (a migration, a new dependency, a gotcha the
     reviewer would otherwise miss). Keep it to about five bullets. -->

## Design

<!-- Why this shape: the constraint or trade-off that decided it, and the
     alternative you rejected. Bullets, one per decision. Docs record only
     current state, so this is where the reasoning lives. Delete for a
     mechanical change. -->

## Testing

<!-- What CI cannot prove. Bullets, one per check: cases exercised by hand and
     where, plus the tests added or updated and anything left uncovered and
     why. CI proves the suite passed, not that the new behaviour is in it.
     A gate the CI workflow already runs is reported by the checks list, so
     this section stays silent about it. -->
