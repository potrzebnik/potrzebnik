# cp .env.example .env

# POSTGRES CONFIG
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=app
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# AUTH CONFIG
BETTER_AUTH_SECRET=your-better-auth-secret #random secret key, store in vault in prod
BETTER_AUTH_URL=http://localhost:3000 # Base URL of the app
GOOGLE_AUTH_ENABLED=false
# Required when GOOGLE_AUTH_ENABLED=true
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret
# Google OAuth redirect URI: ${BETTER_AUTH_URL}/api/auth/callback/google

EMAIL_SENDING_ENABLED=false
# Required when EMAIL_SENDING_ENABLED=true
# RESEND_API_KEY="re_(...)"
# RESEND_FROM_EMAIL=noreply@example.com
# Optional when EMAIL_SENDING_ENABLED=true
# RESEND_FROM_NAME=Potrzebnik
